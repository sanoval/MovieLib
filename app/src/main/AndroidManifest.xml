<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sanov.movielib">

    <uses-permission android:name="android.permission.INTERNET" />
    <permission android:name="com.example.sanov.movielib.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="com.example.sanov.movielib.WRITE_DATABASE" android:protectionLevel="normal"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".view.activities.MovieDetailActivity"
            android:parentActivityName=".view.activities.MainActivity"
            tools:targetApi="jelly_bean" />
        <activity android:name=".view.activities.MovieSearchActivity"
            android:parentActivityName=".view.activities.MainActivity"
            tools:targetApi="jelly_bean"/>

        <provider
            android:authorities="com.example.sanov.movielib"
            android:name=".provider.FavoriteProvider"
            android:exported="true"
            android:readPermission="com.example.sanov.movielib.READ_DATABASE"
            android:writePermission="com.example.sanov.movielib.WRITE_DATABASE"/>
    </application>

</manifest>